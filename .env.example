# Database
# Note: docker-compose uses port 5434 to avoid conflicts with default PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/maintenance_db?schema=public"

# Redis
# Note: docker-compose uses port 6381 to avoid conflicts with default Redis
REDIS_HOST=localhost
REDIS_PORT=6381

# Application
PORT=3000
NODE_ENV=development
